<header class="px-5 bg-dark text-light">
   <nav class="navbar navbar-expand-md navbar-dark  ">
      <div class="collapse navbar-collapse">
         <ul class="navbar-nav">
            <li class="nav-item">
               <a class="nav-link" href="{{ path('main_homepage') }}">Home</a>
            </li>
            <li class="nav-item">
               <a class="nav-link" href="{{ path('main_news') }}">News</a>
            </li>
             {% if is_granted('ROLE_JOURNALIST') %}
                 <li class="nav-item">
                     <a class="nav-link" href="{{ path('main_post_list') }}">Posts</a>
                 </li>
             {% endif %}
         </ul>
      </div>
      <div class="collapse navbar-collapse justify-content-end">
         <ul class="navbar-nav">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('main_logout') }}">Log Out</a>
               </li>
            {% else %}
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('main_registration') }}">Registration</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('main_login') }}">Login</a>
               </li>
            {% endif %}
         </ul>
      </div>
   </nav>
</header>