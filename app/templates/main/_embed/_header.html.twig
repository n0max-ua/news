<header class="px-5 bg-dark text-light">
   <nav class="navbar navbar-expand-md navbar-dark  ">
      <div class="collapse navbar-collapse">
         <ul class="navbar-nav">
             <li class="nav-item mr-5 fs-5">
                 <a class="nav-link" href="{{ path('main_homepage') }}">
                     <strong><i class="fab fa-neos"></i></strong>
                 </a>
             </li>
            <li class="nav-item">
               <a class="nav-link" href="{{ path('main_news') }}">News</a>
            </li>
             {% if is_granted('ROLE_JOURNALIST') %}
                 <li class="nav-item">
                     <a class="nav-link" href="{{ path('main_post_list') }}">Posts</a>
                 </li>
             {% endif %}
         </ul>
      </div>
      <div class="collapse navbar-collapse justify-content-end">
         <ul class="navbar-nav">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_profile_index') }}">
                        {% if app.user.photo %}
                            <img style="height: 30px;width: 30px; border-radius: 50%;" src="{{ asset('uploads/') }}{{ app.user.photo }}"/>
                            {% else %}
                                <img style="max-height: 30px; border-radius: 50%;" src="{{ asset('build/images/no-img.jpg') }}"/>
                        {% endif %}
                    </a>
                </li>
{#                <li class="nav-item">#}
{#                    <i class="nav-link mt-1 fal fa-grip-lines-vertical"></i>#}
{#                </li>#}
{#               <li class="nav-item">#}
{#                  <a class="nav-link" href="{{ path('main_logout') }}"><strong>Log Out</strong></a>#}
{#               </li>#}
            {% else %}
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('main_registration') }}">Registration</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('main_login') }}">Login</a>
               </li>
            {% endif %}
         </ul>
      </div>
   </nav>
</header>